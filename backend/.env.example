# Copy to .env on the machine that runs the backend (never commit it).
# The VPS password lives here; compose + backend-up.sh both read this file.
FG_DB_PASSWORD=change-me-to-something-long-and-random
